#Sherlock tool

sudo apt update && sudo apt full-upgrade -y

pip3 install -r requirements.txt

git clone https://github.com/sherlock-project/sherlock.git

cd sherlock

pipx install sharelock-project / pip install --user sherlock-project

sherlock --version
